<Window x:Class="WisperFlow.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WisperFlow Settings"
        Width="560" Height="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        MinWidth="480" MinHeight="600"
        Background="{StaticResource PrimaryBrush}">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header with accent line -->
        <StackPanel Grid.Row="0" Margin="0,0,0,28">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Width="4" Height="40" CornerRadius="2" Background="{StaticResource AccentBrush}" Margin="0,0,16,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="WisperFlow" 
                               FontFamily="Segoe UI Semibold" FontSize="26" 
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Push-to-talk dictation with AI transcription" 
                               FontFamily="Segoe UI" FontSize="13" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </StackPanel>
        
        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,8,0">
            <StackPanel>
                
                <!-- Hotkey Section -->
                <TextBlock Text="HOTKEY" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Hold this key combination to record:" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="HotkeyTextBox" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="Ctrl + Win" 
                                     IsReadOnly="True"
                                     Cursor="Hand"
                                     PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                     PreviewKeyUp="HotkeyTextBox_PreviewKeyUp"
                                     GotFocus="HotkeyTextBox_GotFocus"
                                     LostFocus="HotkeyTextBox_LostFocus"/>
                            <TextBlock Grid.Column="1" 
                                       Text="Click to change" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="11" FontStyle="Italic"
                                       VerticalAlignment="Center" 
                                       Margin="16,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Command Mode Section -->
                <TextBlock Text="COMMAND MODE" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <CheckBox x:Name="CommandModeCheckBox" 
                                  Style="{StaticResource ModernToggle}"
                                  Content="Enable command mode"
                                  Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Hotkey:" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <TextBox x:Name="CommandHotkeyTextBox" 
                                 Style="{StaticResource ModernTextBox}"
                                 Text="Ctrl + Win + Alt" 
                                 IsReadOnly="True"
                                 Cursor="Hand"
                                 Width="200" HorizontalAlignment="Left"
                                 PreviewKeyDown="CommandHotkeyTextBox_PreviewKeyDown"
                                 PreviewKeyUp="CommandHotkeyTextBox_PreviewKeyUp"
                                 GotFocus="CommandHotkeyTextBox_GotFocus"
                                 LostFocus="CommandHotkeyTextBox_LostFocus"
                                 Margin="0,0,0,12"/>
                        
                        <Border Background="{StaticResource TertiaryBrush}" CornerRadius="8" 
                                Padding="12" Margin="0,0,0,12">
                            <TextBlock TextWrapping="Wrap" FontSize="11">
                                <Run Foreground="{StaticResource TextSecondaryBrush}">Hold the hotkey and speak a command.</Run>
                                <LineBreak/>
                                <Run Foreground="{StaticResource TextSecondaryBrush}">• With text selected: transforms the text (e.g., "make this formal")</Run>
                                <LineBreak/>
                                <Run Foreground="{StaticResource TextSecondaryBrush}">• No selection: opens query in AI chat</Run>
                            </TextBlock>
                        </Border>
                        
                        <TextBlock Text="When no text is selected, open in:" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,4,0,8"/>
                        <ComboBox x:Name="SearchEngineComboBox" Style="{StaticResource ModernComboBox}" Width="200" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
                
                <!-- Code Dictation Section -->
                <TextBlock Text="CODE DICTATION" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <CheckBox x:Name="CodeDictationCheckBox" 
                                  Style="{StaticResource ModernToggle}"
                                  Content="Enable code dictation mode"
                                  Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Hotkey:" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <TextBox x:Name="CodeDictationHotkeyTextBox" 
                                 Style="{StaticResource ModernTextBox}"
                                 Text="Ctrl + Shift" 
                                 IsReadOnly="True"
                                 Cursor="Hand"
                                 Width="200" HorizontalAlignment="Left"
                                 PreviewKeyDown="CodeDictationHotkeyTextBox_PreviewKeyDown"
                                 PreviewKeyUp="CodeDictationHotkeyTextBox_PreviewKeyUp"
                                 GotFocus="CodeDictationHotkeyTextBox_GotFocus"
                                 LostFocus="CodeDictationHotkeyTextBox_LostFocus"
                                 Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Code Model (3B+ recommended):" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <ComboBox x:Name="CodeDictationModelComboBox" 
                                  Style="{StaticResource ModernComboBox}"
                                  SelectionChanged="CodeDictationModel_Changed"
                                  Margin="0,0,0,8"/>
                        <TextBlock x:Name="CodeDictationModelDesc"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Language:" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <ComboBox x:Name="CodeDictationLanguageComboBox" 
                                  Style="{StaticResource ModernComboBox}"
                                  Width="200" HorizontalAlignment="Left"
                                  Margin="0,0,0,12"/>
                        
                        <Border Background="{StaticResource TertiaryBrush}" CornerRadius="8" Padding="12">
                            <TextBlock TextWrapping="Wrap" FontSize="11">
                                <Run Foreground="{StaticResource TextSecondaryBrush}">Hold the hotkey and speak code naturally:</Run>
                                <LineBreak/>
                                <Run Foreground="{StaticResource TextSecondaryBrush}">• "for i in range 10" → </Run>
                                <Run Foreground="{StaticResource AccentBrush}" FontFamily="Consolas">for i in range(10):</Run>
                                <LineBreak/>
                                <Run Foreground="{StaticResource TextSecondaryBrush}">• "my variable equals 5" → </Run>
                                <Run Foreground="{StaticResource AccentBrush}" FontFamily="Consolas">my_variable = 5</Run>
                                <LineBreak/>
                                <Run Foreground="{StaticResource TextSecondaryBrush}">• "define function add that takes a and b" → </Run>
                                <Run Foreground="{StaticResource AccentBrush}" FontFamily="Consolas">def add(a, b):</Run>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- Audio Section -->
                <TextBlock Text="AUDIO" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Microphone" Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <ComboBox x:Name="MicrophoneComboBox" Style="{StaticResource ModernComboBox}"/>
                        
                        <TextBlock Text="Language" Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,16,0,8"/>
                        <ComboBox x:Name="LanguageComboBox" Style="{StaticResource ModernComboBox}"/>
                    </StackPanel>
                </Border>
                
                <!-- AI Models Section -->
                <TextBlock Text="AI MODELS" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Transcription Model" Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="TranscriptionModelComboBox" 
                                      Style="{StaticResource ModernComboBox}"
                                      SelectionChanged="TranscriptionModel_Changed"/>
                            <Button Grid.Column="1" Content="Manage" 
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="16,12"
                                    Margin="12,0,0,0"
                                    Click="ManageModels_Click"/>
                        </Grid>
                        <TextBlock x:Name="TranscriptionModelDesc"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"/>
                        
                        <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,20,0,20" Opacity="0.5"/>
                        
                        <TextBlock Text="Polish Model" Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="PolishModelComboBox" 
                                      Style="{StaticResource ModernComboBox}"
                                      SelectionChanged="PolishModel_Changed"/>
                            <Button Grid.Column="1" Content="Manage" 
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="16,12"
                                    Margin="12,0,0,0"
                                    Click="ManageModels_Click"/>
                        </Grid>
                        <TextBlock x:Name="PolishModelDesc"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Text Processing Section -->
                <TextBlock Text="TEXT PROCESSING" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <CheckBox x:Name="PolishCheckBox" 
                                  Style="{StaticResource ModernToggle}"
                                  Content="Polish output (punctuation, grammar, formatting)"
                                  Margin="0,0,0,16"/>
                        
                        <CheckBox x:Name="NotesModeCheckBox" 
                                  Style="{StaticResource ModernToggle}"
                                  Content="Notes mode (aggressive cleanup, bullets, headings)"/>
                        
                        <Border Background="{StaticResource TertiaryBrush}" CornerRadius="8" 
                                Padding="12" Margin="60,12,0,0">
                            <TextBlock Text="Notes mode removes filler words, creates bullet lists, and formats headings when you speak 'heading:', 'bullet point:', etc."
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="11"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- API Key Section -->
                <TextBlock Text="OPENAI API KEY" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="{StaticResource TertiaryBrush}" 
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1" CornerRadius="8">
                                <PasswordBox x:Name="ApiKeyPasswordBox" 
                                             Background="Transparent"
                                             Foreground="{StaticResource TextPrimaryBrush}"
                                             BorderThickness="0"
                                             Padding="14,12"
                                             FontFamily="Segoe UI"
                                             FontSize="13"
                                             />
                            </Border>
                            <Button Grid.Column="1" 
                                    Content="Save" 
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="20,12"
                                    Margin="12,0,0,0"
                                    Click="SaveApiKey_Click"/>
                        </Grid>
                        <TextBlock Text="Or set OPENAI_API_KEY environment variable. Key is stored securely in Windows Credential Manager."
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap"/>
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Ellipse x:Name="ApiKeyStatusDot" Width="8" Height="8" Margin="0,0,8,0"/>
                            <TextBlock x:Name="ApiKeyStatusText" 
                                       FontSize="12"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Startup Section -->
                <TextBlock Text="SYSTEM" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <CheckBox x:Name="StartupCheckBox" 
                                  Style="{StaticResource ModernToggle}"
                                  Content="Launch WisperFlow at Windows startup"/>
                    </StackPanel>
                </Border>
                
                <!-- Debug Section -->
                <TextBlock Text="TROUBLESHOOTING" Style="{StaticResource SettingLabel}"/>
                <Border Style="{StaticResource CardBorder}" Margin="0,0,0,0">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="View application logs to diagnose issues"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="12" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="View Logs" 
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="16,10"
                                    Click="ViewLogs_Click"/>
                            <Button Grid.Column="2" Content="Open Folder" 
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="16,10"
                                    Margin="8,0,0,0"
                                    Click="OpenLogFolder_Click"/>
                        </Grid>
                        <TextBlock x:Name="LogPathText"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="10" TextWrapping="Wrap" Margin="0,12,0,0"
                                   Opacity="0.7"/>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Footer Buttons -->
        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}" 
                Padding="0,20,0,0" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" 
                        Style="{StaticResource SecondaryButton}"
                        Padding="28,12"
                        Click="Cancel_Click"/>
                <Button Content="Save Changes" 
                        Style="{StaticResource ModernButton}"
                        Padding="28,12"
                        Margin="12,0,0,0"
                        Click="Save_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

