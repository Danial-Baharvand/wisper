<Window x:Class="WisperFlow.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WisperFlow Settings"
        Width="480" Height="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="WisperFlow" 
                       FontFamily="Segoe UI Semibold" FontSize="24" 
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Push-to-talk dictation with AI transcription" 
                       FontFamily="Segoe UI" FontSize="13" 
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Hotkey Section -->
                <TextBlock Text="HOTKEY" Style="{StaticResource SettingLabel}" Margin="0,0,0,8"/>
                <Border Background="{StaticResource SecondaryBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Hold this key combination to record:" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="12" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="HotkeyTextBox" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="Ctrl + Win" 
                                     IsReadOnly="True"
                                     Cursor="Hand"
                                     PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                     GotFocus="HotkeyTextBox_GotFocus"
                                     LostFocus="HotkeyTextBox_LostFocus"/>
                            <TextBlock Grid.Column="1" 
                                       Text="Click to change" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="11" 
                                       VerticalAlignment="Center" 
                                       Margin="12,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Microphone Section -->
                <TextBlock Text="MICROPHONE" Style="{StaticResource SettingLabel}"/>
                <ComboBox x:Name="MicrophoneComboBox" 
                          Style="{StaticResource ModernComboBox}"
                          Margin="0,0,0,20"/>
                
                <!-- Language Section -->
                <TextBlock Text="LANGUAGE" Style="{StaticResource SettingLabel}"/>
                <ComboBox x:Name="LanguageComboBox" 
                          Style="{StaticResource ModernComboBox}"
                          Margin="0,0,0,20"/>
                
                <!-- Polish Settings -->
                <TextBlock Text="TEXT PROCESSING" Style="{StaticResource SettingLabel}" Margin="0,0,0,8"/>
                <Border Background="{StaticResource SecondaryBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <StackPanel>
                        <CheckBox x:Name="PolishCheckBox" 
                                  Style="{StaticResource ModernToggle}"
                                  Content="Polish output (punctuation, grammar, formatting)"
                                  Margin="0,0,0,12"/>
                        
                        <CheckBox x:Name="NotesModeCheckBox" 
                                  Style="{StaticResource ModernToggle}"
                                  Content="Notes mode (more aggressive cleanup, bullets, headings)"
                                  Margin="0,0,0,0"/>
                        
                        <TextBlock Text="Notes mode removes filler words, creates bullet lists, and formats headings when you speak 'heading:', 'bullet point:', etc."
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="54,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- API Key Section -->
                <TextBlock Text="OPENAI API KEY" Style="{StaticResource SettingLabel}" Margin="0,0,0,8"/>
                <Border Background="{StaticResource SecondaryBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <PasswordBox x:Name="ApiKeyPasswordBox" 
                                         Background="{StaticResource SecondaryBrush}"
                                         Foreground="{StaticResource TextPrimaryBrush}"
                                         BorderBrush="{StaticResource BorderBrush}"
                                         BorderThickness="1"
                                         Padding="12,10"
                                         FontFamily="Segoe UI"
                                         FontSize="13"
                                         PasswordChar="●"/>
                            <Button Grid.Column="1" 
                                    Content="Save" 
                                    Style="{StaticResource SecondaryButton}"
                                    Width="70"
                                    Margin="8,0,0,0"
                                    Click="SaveApiKey_Click"/>
                        </Grid>
                        <TextBlock Text="Or set OPENAI_API_KEY environment variable. Key is stored securely in Windows Credential Manager."
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Ellipse x:Name="ApiKeyStatusDot" Width="8" Height="8" Margin="0,0,6,0"/>
                            <TextBlock x:Name="ApiKeyStatusText" 
                                       FontSize="12"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Startup Section -->
                <TextBlock Text="STARTUP" Style="{StaticResource SettingLabel}" Margin="0,0,0,8"/>
                <Border Background="{StaticResource SecondaryBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <CheckBox x:Name="StartupCheckBox" 
                              Style="{StaticResource ModernToggle}"
                              Content="Launch WisperFlow at Windows startup"/>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" 
                    Style="{StaticResource SecondaryButton}"
                    Width="100"
                    Click="Cancel_Click"/>
            <Button Content="Save" 
                    Style="{StaticResource ModernButton}"
                    Width="100"
                    Margin="12,0,0,0"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>

