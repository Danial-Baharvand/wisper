<Window x:Class="WisperFlow.ScreenshotOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screenshot Selection"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Maximized"
        Cursor="Cross"
        KeyDown="Window_KeyDown">
    
    <Grid x:Name="RootGrid">
        <!-- Dark overlay with hole cut out for selection -->
        <!-- 4 Rectangles for "Hole" Overlay -->
        <Rectangle x:Name="MaskTop" Fill="#80000000" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
        <Rectangle x:Name="MaskBottom" Fill="#80000000" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>
        <Rectangle x:Name="MaskLeft" Fill="#80000000" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Rectangle x:Name="MaskRight" Fill="#80000000" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        
        <!-- Selection border -->
        <Border x:Name="SelectionBorder"
                BorderBrush="#ff6b9d"
                BorderThickness="2"
                Background="Transparent"
                Visibility="Collapsed"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"/>
        
        <!-- Instructions text -->
        <TextBlock x:Name="InstructionsText"
                   Text="Click and drag to select area. Press Escape to cancel."
                   Foreground="White"
                   FontSize="14"
                   FontFamily="Segoe UI"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,50,0,0"
                   Opacity="0.8">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="4" Opacity="0.5" ShadowDepth="1"/>
            </TextBlock.Effect>
        </TextBlock>
    </Grid>
</Window>
